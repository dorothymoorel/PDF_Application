[project]
name = "transloka-worker"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "huey>=3.3.2,<4",
]
classifiers = ["Private :: Do Not Upload"]

[build-system]
requires = ["uv_build>=0.11.26,<0.12"]
build-backend = "uv_build"
