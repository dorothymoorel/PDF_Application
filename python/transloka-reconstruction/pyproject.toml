[project]
name = "transloka-reconstruction"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "reportlab>=5.0.1,<6",
]
classifiers = ["Private :: Do Not Upload"]

[build-system]
requires = ["uv_build>=0.11.26,<0.12"]
build-backend = "uv_build"
