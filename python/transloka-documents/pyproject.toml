[project]
name = "transloka-documents"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pypdf[crypto]>=6.14,<7",
]
classifiers = ["Private :: Do Not Upload"]

[build-system]
requires = ["uv_build>=0.11.26,<0.12"]
build-backend = "uv_build"
